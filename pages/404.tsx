// Minimal Pages Router 404 stub required for Next.js 15 output: "export" mode.
// Without this, static generation crashes during /_error prerender.
// All real routing and content uses App Router (app/).
export default function Custom404() {
  return null;
}
